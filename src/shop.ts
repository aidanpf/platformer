import { Message } from "./helpers/messenger";

export const shop = (sprite) => {

    const init = () => {
    };

    const receive = (message: Message) => {

    }

    init();

    return {receive};
};