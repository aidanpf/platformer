// import { messages } from "./messenger";

// export const collisionManager = (app, bump) => {
    
//     const receive = (message) => {
//         if (message.type === messages.bobBeginsOverlapWithBlock) {

//         }
//     };

//     return {receive};
// };